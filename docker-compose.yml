version: '3.9'

networks:
  default:

services:
  cozastore-app:
    build:
      context: .
      dockerfile: Dockerfile

    image: cozastore:v1
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - default
